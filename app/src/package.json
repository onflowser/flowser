{
  "name": "@flowser/app",
  "version": "0.0.1",
  "homepage": "https://flowser.dev",
  "author": {
    "email": "bartolomej.kozorog@gmail.com",
    "name": "Flowser"
  },
  "scripts": {
    "build-electron": "electron-builder -c.extraMetadata.main=build/electron/main.js",
    "build-mac": "yarn run build-electron --mac",
    "build-win": "yarn run build-electron --win",
    "build-linux": "yarn run build-electron --linux",
    "build-all": "yarn run build-electron --linux --mac --win",
    "install:mac:arm": "cd ../node_modules/sqlite3 && ../.bin/node-pre-gyp install --target_arch=arm64 --build-from-source"
  },
  "devDependencies": {
    "electron-notarize": "^1.2.1",
    "electron-icon-maker": "^0.0.5",
    "dotenv": "^16.0.2"
  }
}
